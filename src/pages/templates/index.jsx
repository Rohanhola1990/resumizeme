import { Outlet } from "react-router-dom";

const Template = () => {
    return (
        <>
            Template index
            <br />
            <Outlet />
        </>
    );
}
 
export default Template;