import { Outlet } from "react-router-dom";

const Boards = () => {
    return (
        <>
            Boards index
            <br />
            <Outlet />
        </>
    );
}
 
export default Boards;